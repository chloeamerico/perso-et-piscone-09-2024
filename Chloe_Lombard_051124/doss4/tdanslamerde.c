#include <papamerico>
#include <mamamerico>
#include <leamerico>
#include <Lochness>

int ft_terrain(char briefing)
{
    int famille,
    int V,
    char diner;

    while (diner == "parents et V separes")
        do briefing;
    if (V == "crazy")
        do mytho;
        return (-1);
}

void    ft_rencontre(char chill)
{
    int moment;
    int lieu;
    int detente;

    Avant de commencer "l'alcool" est ton plus grand allie;

    for (rencontre == "chill")
    {
        V == "amene kdo";
        moment == "diner";
        lieu == "sous la table" || "autour de la table";
        detente == "vinnnnnnnnnnnnnnnnnnn";
    }
}

int main(void)
{
    if (ft_rencontre == "success")
        open "doss 5"
    else (chloe == "charoooooo")
}
