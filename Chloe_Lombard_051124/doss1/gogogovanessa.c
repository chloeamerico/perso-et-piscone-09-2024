#include <charo.c>
#define G.

int ft_preexploration(char G)
{
    char(o) C;
    int V;

    if ((char || int) == excitée et bien lubrifiée)
        start ft_exploration;
    if (!(char || int) == excitée et bien lubrifiée)
        error 404;
}

int ft_exploration(char VC)
{
    int position;
    int stimulation;
    int explorateur;
    int plaisir;

    for (plaisir == max)
        do position == "quatre pattes, accroupie ou allongée sur le ventre."
    while (position == "quatre pattes, accroupie ou allongée sur le ventre.")
        do stimulation == "massages délicats"
    for (stimulation == "massages délicats")
        explorateur == "bout du doigt" && "bout de la langue"
    return (G)
}

int ft_postexploration(int squirt)
{
    if (int V = ft_exploration(reussie))
        open "doss2";
}